{
  "name": "@gestor/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --enable-source-maps --loader ts-node/esm src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/main.js",
    "start:prod": "node --enable-source-maps dist/main.js",
    "lint": "echo 'skip'"
  },
  "dependencies": {
    "@gestor/shared": "*"
  }
}
